package com.workshop;


import com.workshop.Workshop;
import org.junit.jupiter.api.Test;
import static org.hamcrest.MatcherAssert.assertThat;
import static org.hamcrest.Matchers.*;

class WorkshopTest {
  @Test
  void test() {
    assertThat(true, is(false));
  }
}